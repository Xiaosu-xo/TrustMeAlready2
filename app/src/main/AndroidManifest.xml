<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mfsx.xposed.trustmealready"
    android:versionCode="10"
    android:versionName="2.8.0">

    <application
        android:allowBackup="true"
        android:label="@string/app_name">

        <!-- Xposed / LSPosed module declaration -->
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
        <meta-data
            android:name="xposeddescription"
            android:value="v2.8: All 9 modules - SSL, VPN, dev options, debugger, emulator, user cert, network security, SafetyNet, anti-Frida (conditional). Added TLS 1.0-1.3 protocol enforcement (SSLContext.getInstance redirect + SSLSocket/SSLEngine setEnabledProtocols force-all). Fixed WeChat in-app WebView redirect. Android 5-16." />

        <!-- LSPosed scope: apply to all apps (user selects in LSPosed Manager) -->
        <!-- To restrict to specific apps, add a string-array resource named "xposed_scope" -->

    </application>

</manifest>
